if (typeof garbage === "undefined" || garbage === null || garbage === "") {
    return;
}
let formattedText;
if (garbage) {
    formattedText = "Success";
} else {
    formattedText = "Error";
}
return formattedText;
