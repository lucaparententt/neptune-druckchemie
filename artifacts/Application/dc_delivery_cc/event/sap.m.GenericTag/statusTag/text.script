if (typeof status === "undefined" || status === null || status === "") { return; }  

var formattedText= modelMasterData.getData().deliveryStatus.find(dS => dS.key == status).value

return formattedText;